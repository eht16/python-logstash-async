version: 2

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
    - method: pip
      path: sphinx-rtd-theme

build:
  os: ubuntu-22.04
  tools:
    python: '3.12'
